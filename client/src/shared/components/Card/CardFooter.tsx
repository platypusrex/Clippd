import * as React from 'react';
import { View } from 'react-native';

interface Props {
  actionButtons: Array<React.ReactNode>
}

export const CardFooter: React.SFC<Props> = (props) => {
  return (
    <View style={{marginTop: 15}}>

    </View>
  );
};
